#pragma once
#include <SDL.h>

/*class Player
{
	public:
		void Init();
		void Load();
		void Events();
		void Update();
		void Draw();
		void Clean();
	private:
		SDL_Texture* Player_GoingUpTexture = nullptr;
		SDL_Texture* Player_GoingDownTexture = nullptr;
		SDL_Texture* Player_GoingRightTexture = nullptr;
		SDL_Texture* Player_GoingLeftTexture = nullptr;

		SDL_Rect PlayerSrcRect = { 0, 0, 0, 0 };
		SDL_Rect PlayerDstRect = { 0, 0, 0, 0 };

		const double PlayerSpeed = 1.0f;

		long double XPos = 200.0f;
		long double YPos = 200.0f;

		long double oldXPos = 0.0f;
		long double oldYPos = 0.0f;
};
*/