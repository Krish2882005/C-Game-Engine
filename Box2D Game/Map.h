#pragma once
#include <string>
#include <SDL.h>
#include <vector>

/*class Map
{
public:
	void Load();
	void LoadMap(std::vector<std::vector<int>> LevelToLoad, int RowsToLoad, int ColumnsToLoad);
	void Init();
	void Draw();
	void Clean();
private:
	int Rows = 0;
	int Columns = 0;
	short unsigned int CurrentBlockLoader = 0;

	//Beach Textures
	SDL_Texture* Water = nullptr;
	SDL_Texture* Sand = nullptr;

	SDL_Rect WaterSrcRect = { 0, 0, 0, 0 };
	SDL_Rect WaterDstRect = { 0, 0, 0, 0 };
	SDL_Rect SandSrcRect = { 0, 0, 0, 0 };
	SDL_Rect SandDstRect = { 0, 0, 0, 0 };

	std::vector<long double> MapXPosition;
	std::vector<long double> MapYPosition;
};
*/